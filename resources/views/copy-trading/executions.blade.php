<x-user-layout><x-slot name="header">Copy Trading History</x-slot><div class="ui-page max-w-[1280px]"><section class="ui-page-header"><div><p class="ui-kicker">Trading Intelligence</p><h1 class="ui-heading">Copy executions</h1><p class="ui-lead">Every mirrored provider action and its result.</p></div><a href="{{ route('copy-trading.marketplace') }}" class="ui-btn ui-btn-secondary">Back to Copy Trading</a></section><section class="ui-panel overflow-hidden"><div class="overflow-x-auto"><table class="min-w-full"><thead class="ui-table-head"><tr><th class="px-5 py-3">Provider</th><th class="px-5 py-3">Trade</th><th class="px-5 py-3">Requested</th><th class="px-5 py-3">Executed</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Time</th><th class="px-5 py-3"></th></tr></thead><tbody>@forelse($items as $item)<tr class="ui-table-row"><td class="px-5 py-4 text-sm">{{ $item->relationship->provider->copyTraderProfile->strategy_name ?? $item->relationship->provider->name }}</td><td class="px-5 py-4 text-sm font-medium">{{ strtoupper($item->action) }} {{ $item->providerTrade->stock->symbol ?? '—' }}</td><td class="px-5 py-4 text-sm">{{ format_currency($item->requested_amount) }}</td><td class="px-5 py-4 text-sm">{{ format_currency($item->executed_amount) }}</td><td class="px-5 py-4"><span class="ui-status {{ $item->status==='completed'?'ui-status-success':($item->status==='failed'?'ui-status-danger':'ui-status-warning') }}">{{ ucfirst($item->status) }}</span>@if($item->failure_reason)<p class="mt-1 max-w-xs text-xs text-muted-foreground">{{ $item->failure_reason }}</p>@endif</td><td class="px-5 py-4 text-xs text-muted-foreground">{{ $item->executed_at?->format('M d, Y · h:i A') }}</td><td class="px-5 py-4 text-right"><a href="{{ route('copy-trading.executions.show',$item) }}" class="ui-btn ui-btn-secondary">View</a></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center text-sm text-muted-foreground">No copy executions yet.</td></tr>@endforelse</tbody></table></div>@if($items->hasPages())<div class="border-t border-border p-4">{{ $items->links() }}</div>@endif</section></div></x-user-layout>
