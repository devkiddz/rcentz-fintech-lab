<x-user-layout><x-slot name="header">{{ $product->name }}</x-slot><div class="ui-page max-w-5xl"><section class="ui-page-header"><div><p class="ui-kicker">AI Trading Bots</p><h1 class="ui-heading">{{ $product->name }}</h1><p class="ui-lead">{{ $product->description }}</p></div><a href="{{ route('ai-bots.marketplace') }}" class="ui-btn ui-btn-secondary">Marketplace</a></section>
<div class="grid gap-5 md:grid-cols-2"><div class="ui-panel p-6 space-y-4"><div class="flex justify-between"><span class="text-muted-foreground">Asset</span><strong>{{ $product->stock->symbol }}</strong></div><div class="flex justify-between"><span class="text-muted-foreground">Strategy</span><strong>{{ strtoupper($product->strategy) }}</strong></div><div class="flex justify-between"><span class="text-muted-foreground">Action</span><strong>{{ ucfirst($product->action) }}</strong></div><div class="flex justify-between"><span class="text-muted-foreground">Risk</span><strong>{{ ucfirst($product->risk_level) }}</strong></div><div class="flex justify-between"><span class="text-muted-foreground">Minimum balance</span><strong>{{ format_currency($product->minimum_balance) }}</strong></div></div>
<div class="ui-panel p-6"><p class="text-xs text-muted-foreground">Access price</p><p class="mt-1 text-3xl font-semibold">{{ $product->price>0?format_currency($product->price):'Free' }}</p><p class="mt-1 text-sm text-muted-foreground">{{ str_replace('_',' ',$product->billing_period) }}</p>@if($existing)<div class="mt-5 money-note">You already have access to this bot.</div><a class="ui-btn ui-btn-primary mt-4" href="{{ route('ai-bots.my-bots') }}">Open My Bots</a>@else<form class="mt-5" method="POST" action="{{ route('ai-bots.subscribe',$product) }}">@csrf<button class="ui-btn ui-btn-primary w-full">Buy / Subscribe</button></form>@endif</div></div></div></x-user-layout>