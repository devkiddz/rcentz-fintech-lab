<x-admin-layout><x-slot name="header">AI Trading Bots</x-slot><div class="space-y-5"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-semibold">Bot Catalog</h1><p class="text-sm text-muted-foreground">Admin-owned bot products available to customers.</p></div><a href="{{ route('admin.ai-bots.create') }}" class="ui-btn ui-btn-primary">Create Bot</a></div><div class="ui-table-shell overflow-x-auto"><table class="w-full text-sm"><thead class="ui-table-head"><tr><th class="p-4">Bot</th><th>Stock</th><th>Price</th><th>Subscribers</th><th>Status</th><th></th></tr></thead><tbody>@foreach($products as $p)<tr class="ui-table-row"><td class="p-4 font-medium">{{ $p->name }}</td><td>{{ $p->stock->symbol }}</td><td>{{ format_currency($p->price) }}</td><td>{{ $p->subscriptions_count }}</td><td>{{ $p->is_active?'Active':'Inactive' }}</td><td class="p-4 flex gap-2"><a class="ui-btn ui-btn-secondary" href="{{ route('admin.ai-bots.edit',$p) }}">Edit</a><form method="POST" action="{{ route('admin.ai-bots.toggle',$p) }}">@csrf @method('PATCH')<button class="ui-btn ui-btn-secondary">{{ $p->is_active?'Disable':'Enable' }}</button></form></td></tr>@endforeach</tbody></table></div>{{ $products->links() }}</div></x-admin-layout>