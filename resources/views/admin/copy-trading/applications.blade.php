<x-admin-layout><x-slot name="header">Copy Trading Applications</x-slot><div class="space-y-5"><div><h1 class="text-2xl font-semibold">Provider Applications</h1><p class="text-sm text-muted-foreground">Approve who may publish copyable strategies.</p></div><div class="ui-table-shell overflow-x-auto"><table class="w-full text-sm"><thead class="ui-table-head"><tr><th class="p-4">Applicant</th><th>Name</th><th>Risk</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($applications as $a)<tr class="ui-table-row"><td class="p-4">{{ $a->user->name }}</td><td>{{ $a->display_name }}</td><td>{{ ucfirst($a->risk_level) }}</td><td>{{ ucfirst($a->status) }}</td><td class="p-4">@if($a->status==='pending')<div class="flex gap-2"><form method="POST" action="{{ route('admin.copy-trading.applications.approve',$a) }}">@csrf<button class="ui-btn ui-btn-primary">Approve</button></form><form method="POST" action="{{ route('admin.copy-trading.applications.reject',$a) }}">@csrf<textarea name="admin_notes" class="ui-input mb-2" placeholder="Rejection reason" required></textarea><button class="ui-btn ui-btn-secondary">Reject</button></form></div>@endif</td></tr>@endforeach</tbody></table></div>{{ $applications->links() }}</div></x-admin-layout>